import React from 'react'

const Button = ({ count }) => {
    return (
        <div>
            <button>i am a button <span>count={count}</span></button>
        </div>
    )
}

export default Button
